<div class="container">
  <h1>Discover</h1>
  <div class="row">
    <div class="col-xs-12 col-sm-4">
      <%= render "components/card" %>
    </div>
    <div class="col-xs-12 col-sm-4">
      <% @artworks.each do |artwork| %>
        <%= render "components/card", artwork: artwork %>
      <% end %>
    </div>
  </div>
</div>

